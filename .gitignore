build/
build-native/
build-rk3588/
build-mac/
# Build output is ignored, EXCEPT the published reference sample bundle (out/books/lighthouse),
# which is committed as the canonical book-to-data output for provisioning/reference.
# (out/books/aladdin stays ignored — copyrighted scanned fixture, see below.)
out/*
!out/books
out/books/*
!out/books/lighthouse
out_loopback.log
*.o
*.a
compile_commands.json
.cache/
.DS_Store

# Local-only internal design doc — kept out of the repo
AudioBox_DSP_Framework_SDS.md

# Claude Code session data
.claude/

# Local-only architecture/design docs — kept on disk, out of the repo
docs/abox_pipewire.md
docs/story_agent_SDS.md

# Python (studio/)
__pycache__/
*.pyc

# Copyrighted scanned-book fixture — local dev only, never commit
LADYBIRD_Alladin-1.pdf
